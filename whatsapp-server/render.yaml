services:
  - type: web
    name: whatsapp-bulk-service
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: API_KEY
        generateValue: true

